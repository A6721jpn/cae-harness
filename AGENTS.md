# FEBio CAE Harness 開発契約

## 目的

- 最終像：ユーザーがCADファイルと解析条件を与えるだけで、LLMが構造解析を実行し、結果を簡潔に自然言語で報告する「CAEハーネス」。
- いま作っているのはそのMVPである。MVPの範囲は設計仕様書 §1（平面・直方体治具・線形弾性、条件は型付きJSON、LLM入力は対象外）、完了条件は計画書 §2 の8手順。最終像を理由にMVPの範囲を広げない。

## 文書の使い分け

- `docs/plans/mvp-handoff.md`：実装に着手するとき最初に読む（作業場所、環境、タスクT0〜T6のファイル別指示）。
- `docs/plans/2026-09-14-febio-cae-harness-greenfield-plan.md`：完了条件・現在地・backlog を確認するとき。
- `docs/specs/2026-09-14-febio-llm-cae-harness-design-v2.md`：入出力・状態・終了コード・品質項目の約束を変える／確認するとき。
- `docs/specs/implementation-notes.md`：数値上限・ログ構文・接続詳細。コード変更に合わせて更新し、製品契約を変えない限り仕様書は触らない。
- 作業ブランチは `orca/acceptance-integration`、作業ツリーは `C:/Users/backo/orca/workspaces/CAE-HARNESS-V2/acceptance-integration`。

## 確認なしでやってよいこと

- 標準テスト（`python -m pytest`）、`ruff`、`mypy`、`scripts/scan_cae_data.py`、`python -m build` は使い捨ての一時領域で動き、本番データにアクセスしない。実行し、依頼に起因する失敗を直し、影響する試験を再実行してよい。
- 合成STEP（`.local/synthetic/` や引き継ぎ書に載っている合成入力）に対する実FEBio・Gmsh の実行は、引き継ぎ書の予算内なら確認不要。
- 作業ブランチへのコミット。

## 確認が必要なこと

- 実CAEモデル（`02_CAE` 配下）への読み書き、実モデルでの解析、`git push`、他ブランチ・リモートの変更、予算の拡張。
- 材料・荷重・拘束・接触・評価領域などの物理条件が根拠から確定できないとき。形状や慣例から推測せず、`ASK_AND_BLOCK` で質問する。
- 実CAEモデル・結果・認証情報・作業機のローカルパスは Git に入れない。

## 進め方と完了の定義

- MVPとして最小の実装にする。ユーザーが製造レベルの品質を明示的に求めない限り、試験は変更した契約の分だけ書く。
- 1タスクは「実装 → 影響する試験と静的検査を通す → 動かして結果を確認し失敗を直す → 未コミット差分のない候補にする」まで進めてから報告する。途中でレビューを求めない。
- モデル・推論強度は固定せず、実行環境（OMP）のユーザー役割設定に従う。共有変更の競合、長時間試験や solver の重複実行を避ける。
- V2 への統合はユーザーの指示があったときだけ行い、強制更新はしない。

## 報告と証拠の規則

- 報告には対象コミット、変更ファイル、実行したコマンドと終了コード・件数、実FEBio・Studio の起動回数、未検証事項、次に必要な判断を書く。`docs/reviews/` に残す記録も同じ項目。
- 状態語を混同しない。`INSPECTED` は観測完了、`PROVISIONED` は登録完了、`PREPARED` は準備記録の公開、`SUCCEEDED` は終端到達と出力完全性、`LAUNCHED` は表示ソフトの起動、`COMPLETE` は品質・表示を含む完了。終了コード0は操作の成功であり、解析の完了ではない。
- 未実装・証拠不足・範囲外の品質項目は `UNVERIFIED` のまま残す。合成データや模擬ログの合格を、実FEBio・Studio・実モデルの成功証拠にしない。
- 最終完成は、必須実E2Eすべてと最終BottomFrame実モデルE2Eの合格証拠がそろうまで宣言しない。MVP完了は計画書 §2 の8手順で別に判定する。
